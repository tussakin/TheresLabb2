@model IEnumerable<Labb2Theres.Models.StudentYear>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentYearName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentYearName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StudentYearId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StudentYearId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StudentYearId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
